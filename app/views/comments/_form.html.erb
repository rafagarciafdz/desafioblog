<div class="new-comment" style="background-color:gray">
	<%= form_for [@post, @comment], remote: true do |f| %>
		<%= f.label :comment %>
		<%= f.text_area :comment %>
		<%= f.submit "Enviar" %>
	<% end %>
</div>
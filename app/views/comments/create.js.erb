<% if @comment.persisted? %>
	$("#comments_count").text("<%= @post.comments.count %>");
	$("#comments").append("<%= j render @comment %>");
	$("textarea").val("")
<% else %>
	alert("Tu comentario no pudo ser guardado.");
<% end %>